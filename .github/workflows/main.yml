name: Build-Marble
on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Setup Toolchain
        run: sudo apt install -y build-essential gcc-aarch64-linux-gnu
      
      - name: Get Kernel Source
        run: |
          git clone --depth=1 https://github.com/LowTension/android_kernel_xiaomi_sm8475 kernel
          cd kernel
          # Ищем файл конфигурации, который содержит 'marble' в названии
          CONFIG_FILE=$(find arch/arm64/configs -name "*marble*defconfig" | head -n 1)
          echo "Using config: $CONFIG_FILE"
          make ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- $(basename $CONFIG_FILE)
          
      - name: Build Driver
        run: |
          git clone --depth=1 https://github.com/morrownr/8812au-20210820 driver
          cd driver
          make ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- KSRC=../kernel
          
      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: 8812au-marble
          path: driver/8812au.ko
